services:
  frontend:
    container_name: vehicle-registration-frontend
    build:
      context: .
      dockerfile: Dockerfile
    image: vehicle-registration-frontend:latest
    ports:
      - "8088:80"
    restart: unless-stopped

